# Personal data — never commit (mail history, receipts, tokens, backups)
data/
קבלות/
ארכיון/
גיבויים/
הורדות/
יומני אוטומציה/

# Build output
*.exe
build/
dist/
*.spec

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# The Google sign-in key built into MailBrief.exe — local builds only (the release gets it from a GitHub secret)
google_client.json
